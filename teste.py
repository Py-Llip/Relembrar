"""Excelente, Fellipe! Bora ativar essa mente de programador com desafios que vão do **intermediário** ao **avançado** em Python. São exercícios que envolvem listas, dicionários, funções, orientação a objetos, recursão, decoradores, geradores, e até um pouco de multithreading.

---

### 🔹 Nível Intermediário

1. **Listas e Condições**

```python
# Filtrar todos os números pares e positivos de uma lista e elevar ao quadrado.
numeros = [10, -3, 4, -7, 0, 8, 13, -2]
# Faça: lista_pares_quadrado = [...]
```

2. **Função com Parâmetro Variável**

```python
# Crie uma função que aceita qualquer quantidade de números e retorna a média deles.
# def media(*args): ...
```

3. **Dicionários e Manipulação**

```python
# A partir da lista de tuplas, crie um dicionário onde a chave é o nome e o valor é a idade.
dados = [('João', 23), ('Maria', 19), ('Ana', 21)]
```

4. **Função Recursiva**

```python
# Implemente uma função recursiva que calcule o fatorial de um número.
# def fatorial(n): ...
```

5. **Funções com Funções**

```python
# Crie uma função que recebe outra função como parâmetro e aplica ela aos elementos de uma lista.
# def aplicar_funcao(lista, func): ...
```

---

### 🔸 Nível Avançado

6. **Classes e Objetos**

```python
# Crie uma classe Produto com atributos nome, preço e método aplicar_desconto().
# instancie produtos e aplique descontos diferentes.
```

7. **Decoradores**

```python
# Crie um decorador que mede o tempo que uma função leva para executar.
import time
# def cronometro(func): ...
```

8. **Geradores**

```python
# Crie um gerador que retorna infinitamente os números da sequência de Fibonacci.
# def fibonacci(): ...
```

9. **List Comprehension + Condicional Complexa**

```python
# Gere uma lista de strings com o formato "par-<número>" para todos os números pares entre 1 e 50.
# Resultado esperado: ['par-2', 'par-4', ..., 'par-50']
```

10. **Threading**

```python
# Crie duas funções que imprimem contagens diferentes. Execute-as simultaneamente usando threads.
import threading
# def contar1(): ...
# def contar2(): ...
```

---

### 🌟 Desafio Final: Sistema de Notas

Crie um pequeno sistema de alunos e notas:

* A classe `Aluno` deve armazenar nome e notas.
* Deve haver um método para calcular a média e outro para verificar aprovação (nota ≥ 7).
* Adicione tratamento de exceções caso seja inserido um valor inválido como nota.

---

Se quiser, posso corrigir suas respostas ou fazer os exercícios junto com você. Posso também gerar uma versão PDF com todos eles bonitinhos. Vamos nessa?
"""